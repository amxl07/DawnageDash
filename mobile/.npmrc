# @react-native-community/datetimepicker carries an optional peer on
# react-native-windows, which pins a different react-native than the Expo SDK.
# We never build for Windows, so relax peer resolution rather than drop the picker.
legacy-peer-deps=true
